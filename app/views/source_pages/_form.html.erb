<%= form_with(model: source_page) do |form| %>
  <% if source_page.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(source_page.errors.count, "error") %> prohibited this source_page from being saved:</h2>

      <ul>
        <% source_page.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :url, style: "display: block" %>
    <%= form.text_field :url %>
  </div>

  <div>
    <%= form.label :source_id, style: "display: block" %>
    <%= form.text_field :source_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
